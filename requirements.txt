# GibridSim talab qiladigan kutubxonalar.
# Python 3.10+ uchun mos. Frontend (Bootstrap, Plotly) CDN orqali yuklanadi,
# shuning uchun bu yerda faqat backend bog'liqliklari.

Django>=5.0,<6.0
numpy>=1.26
scipy>=1.11

# Ishlab chiqarish (deploy) uchun: WSGI server, statik fayllar, baza URL.
gunicorn>=21.2
whitenoise>=6.6
dj-database-url>=2.1
psycopg2-binary>=2.9   # ixtiyoriy PostgreSQL uchun (DATABASE_URL berilsa)

# Testlar uchun (ishlab chiqarishda shart emas):
pytest>=8.0
pytest-django>=4.8
